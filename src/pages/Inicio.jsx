import HeroSection from '../components/HeroSection';

const Inicio = () => {
  return (
    <section id="inicio" className="intro-box">
      <HeroSection />
    </section>
  );
};

export default Inicio;